# Profile: software

The base profile. General application/library/tool development.

## Adds to the loop

Nothing — the base loop is the software loop.

## Gate

`scripts/gate.sh` auto-detection is sufficient; add project-specific checks as `scripts/gate.d/*.sh`.

## Declarations required at /init

- None beyond ARCHI.md.
