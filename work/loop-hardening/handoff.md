You are the implementer for this work unit. Read AGENTS.md in the repo root first — it is your contract.

Work unit: work/loop-hardening/plan.md  (read it in full; it is your source of truth)
Branch: wt/loop-hardening (already checked out in this worktree — verify with `git branch --show-current` before changing anything)

Footprint (from the plan, repeated here as the hard boundary):
- scripts/release.sh
- scripts/codex-review.sh
- scripts/agent-exec.sh
- skills/5-retro/SKILL.md
- tests/test-scripts.sh
- PLAN.md (only: retire the reviewer.command Risks bullet, add one Decisions line, refresh the Next-candidate line)
- Do NOT touch: scripts/worktree.sh, scripts/fan-exec.sh, CLAUDE.md, README.md, .claude/agents/*, AGENTS.md, skills/2-implement/SKILL.md, knowledge/, ARCHI.md (ARCHI.md is regenerated by /compact at release time, not by you)

Key constraints:
- Fix 1 (release.sh, check_previous_retro): if `git show origin/main:work/.last-released` succeeds, the checkout's marker must be byte-identical — compare with `git show origin/main:work/.last-released | cmp -s - work/.last-released`, NOT command substitution (substitution strips trailing newlines). Deleted marker → die with a message containing `marker deleted`. Content difference → die with a message containing `marker differs from origin/main` and also the guidance `if this branch already carries its release commit, merge its PR instead of re-running release`. No fetch and no origin/main-missing fallback inside the guard: check_origin_main_ancestor (line ~229) has already fetched and died if origin/main is missing before check_previous_retro runs. Bootstrap (git show fails on the known-existing ref): keep today's pass-through, but die with a message containing `malformed` if a local marker exists and is empty or multi-line.
- Fix 2 (codex-review.sh): read reviewer.command from `main:config.yaml` instead of `$branch:config.yaml`. Keep the awk extraction and everything else (exit-code contract, artifact handling, verdict parsing) unchanged. Update error messages to name main, not the branch.
- Fix 3 (agent-exec.sh): record the worktree's HEAD before dispatch. After the implementer command exits 0, exit non-zero with a distinct stderr message containing `no new commit` ONLY if HEAD is unchanged AND `git status --porcelain` (in the worktree) is empty — i.e. the dispatch produced literally nothing. Uncommitted changes with unchanged HEAD must remain exit 0. Do NOT otherwise restructure agent-exec.sh and do NOT touch its config-parsing awk line (known-latent lookahead bug, explicitly out of scope). Note the script is `set -euo pipefail`, so capture the eval's exit status carefully.
- Fix 4 (skills/5-retro/SKILL.md, prose only): step 1 gains the literal sentence `If the worktree already exists, reuse it instead of running worktree.sh add again` (worktree.sh add hard-fails on an existing path). Add a final step that runs `worktree.sh remove retro-<slug>` after the PR merges. Grep targets that must exist verbatim: `reuse it instead` and `worktree.sh remove`.
- PLAN.md: delete the Risks bullet beginning "`reviewer.command` is executable branch content"; add one Decisions line: `- 2026-08-26 — codex reviewer command is main-bound (branch config cannot choose the reviewer binary); marker byte-identity vs origin/main — work/loop-hardening`; update the Now section's loop-hardening line if it claims candidate status.

Tests (tests/test-scripts.sh), all hermetic in the existing fixture style:
- Marker tests over the release fixture: (a) marker on origin/main but deleted on branch → die, stderr contains `marker deleted`; (b) marker mutated to a different retro-complete slug → die, stderr contains `marker differs`; (c) marker with a trailing extra newline vs origin/main's → die `marker differs` (proves byte comparison); (d) re-run on a branch already carrying its release commit → die, stderr names the already-released guidance; (e) bootstrap (no marker on origin/main) with empty local marker → die `malformed`; (f) same with multi-line local marker → die `malformed`. The existing marker/retro matrix cases must pass with their assertions unmodified.
- codex-review tests: setup_codex_review_fixture currently writes config.yaml only inside the worktree per command_mode — move the command_mode config-writing so fixture main carries config.yaml before wt/demo is cut (hoist the case into the primary before `git branch wt/demo`). Add a new mode: main carries an approve command, wt/demo carries a request-changes override → expect exit 0 (branch override has no effect). Missing `reviewer:` on fixture main → exit 2. All existing codex exit-code/stderr matrix cases keep their assertions. Replace the structural check `codex-review reads config from branch` (grep for `git show "$branch:config.yaml"`) with a structural check that it reads `main:config.yaml`.
- agent-exec tests (new fixture; agent-exec today has only one argument-validation test): canned implementer that commits → exit 0; canned implementer that leaves uncommitted changes without committing → exit 0; canned implementer that exits 0 touching nothing → non-zero, stderr contains `no new commit`.
- Existing hermetic fan-exec cases must pass unmodified.

When done:
1. Run scripts/gate.sh from the repo root — it must pass.
2. Commit your work on this branch with a clear message.
3. Print a final summary: what changed and why, criteria partially met (if any), out-of-scope observations.
