You are the implementer for this work unit. Read AGENTS.md in the repo root first — it is your contract.

Work unit: work/work-profile/plan.md  (read it in full; it is your source of truth — it is present in this worktree)
Branch: wt/work-profile (already checked out in this worktree — verify with `git branch --show-current` before changing anything)

## Footprint (from the plan — the hard boundary)

Modify:
- `config.yaml` — add a `review:` block with `human_pr_review: self` (values `self | platform-team`); add `work` to the valid `profile:` values in its comment; add the Claude implementer `command:` as a commented alternative under `implementer:`. Do NOT change the template's own active values (`profile: software`, `implementer.runtime: codex`).
- `scripts/release.sh` — add a config-selected release topology (details below).
- `tests/test-scripts.sh` — add platform-team coverage (details below).
- `skills/4-release/SKILL.md` — steps 3 AND 4 branch on `review.human_pr_review`.
- `skills/init/SKILL.md` — add `work` to the hardcoded profile enum (line ~11) and add one line stating profile prose drives one-time infra swaps.

Add:
- `profiles/work.md`
- `bitbucket-pipelines.yml`

Do NOT touch: `scripts/agent-exec.sh` (already reads `implementer.command` generically — the Claude swap is pure config), `ARCHI.md` (regenerated by /compact, not here), and the template's own active config values.

## Key constraints

**release.sh — the core change.** Read `review.human_pr_review` from `config.yaml` with naive awk (same style as `scripts/agent-exec.sh:17` and `scripts/worktree.sh:13`). Default to `self` if unset. Shared preconditions (verdict, gate, ARCHI fresh, version increasing, clean release worktree) run in BOTH modes. The `main` worktree lookup (`release.sh:198-199`) STAYS required in both modes — worktrees are cut from it.

- `self` mode → behavior must be **byte-for-byte unchanged** from today (existing tests must still pass): local-main ff precondition + ff-merge into local `main`; the "stopped before push" message stays.
- `platform-team` mode →
  - Run a fetch + ancestor guard: `git fetch origin main`, then require `origin/main` is an ancestor of the release branch (`git merge-base --is-ancestor origin/main <release-branch>`); die with a clear "rebase/sync your branch onto origin/main" message otherwise.
  - This guard must run TWICE — once BEFORE `check_gate` and once AFTER — mirroring the existing `check_ff_possible` double-call at `release.sh:213,221`. Rationale: the gate takes real time and another PR can merge into shared `origin/main` mid-run; a single check would let two near-simultaneous releases compute the same version.
  - Compute `next_version` from the release branch's `VERSION` (read once, as self-mode does at `release.sh:208` — the guard never rebases, so VERSION is stable between the two checks). Do not over-engineer relocation of the computation.
  - Commit the VERSION+CHANGELOG bump on the release branch. Do NOT ff-merge `main`. Do NOT create a `v<version>` tag. Replace the local-main ff precondition with the origin/main-ancestor guard.
  - Print the release branch name and a "push this branch, open a Bitbucket PR, platform engineer merges" message.
  - The fetch is read-only — release.sh still never pushes (invariant #5 holds).

**Tagging in platform-team mode is permanently out of scope** — never tag; `VERSION` (not tags) is the source of truth for `next_version`. Document the manual post-merge command in `profiles/work.md`.

**tests/test-scripts.sh** — reuse the existing bare-remote fixture (`setup_release_fixture` already creates a bare `origin`). Add:
  1. platform-team single-release: `main` untouched, no `v<version>` tag, bump commit present on the release branch.
  2. sequential two-release collision: release A, simulate the platform-team PR merge into remote `main`, then attempt B off a stale local `main` — release.sh must REFUSE until B's branch contains `origin/main`; after sync/rebase, B computes the next micro (no collision).
  3. mid-gate move: `origin/main` moves DURING the gate run (e.g. a temporary `gate.d/` hook that pushes to the bare `origin`) — must be caught by the post-gate re-check.
  Keep all existing self-mode assertions passing.

**skills/4-release/SKILL.md** — step 3 (exit-code report) and step 4 (push) both branch on `review.human_pr_review`. self: report prepared version/tag/local-main advanced, push `main` + tag. platform-team: report bump-commit-on-branch/no-tag/main-untouched, push the release branch and print PR instructions, never push `main`.

**skills/init/SKILL.md** — add `work` to the profile enum; add one line: a profile's one-time infra swaps (Bitbucket CI, implementer runtime) happen by the orchestrator following `profiles/work.md` prose — that prose is the mechanism.

**profiles/work.md** — document: the Claude implementer `command:`; both `human_pr_review` topologies and what each does; the CI swap (add `bitbucket-pipelines.yml`, delete `.github/workflows/ci.yml`); the platform-team sync workflow (release.sh fetches and refuses on a stale branch; keep local `main` pulled from the PR-merged remote — note the required `main` worktree check does NOT imply local `main` is current, since the guard checks `origin/main`); the permanent no-tag limitation + the manual `git tag vX.Y.Z <merge-commit> && git push origin vX.Y.Z` command; and Jira as read-only reference only (never created). Match the tone/structure of the existing `profiles/machine-learning.md` and `profiles/software.md`.

**bitbucket-pipelines.yml** — mirror `.github/workflows/ci.yml` STEP-FOR-STEP (it is eyeball-verified, no runner here): install shellcheck, set a git identity (`user.email`/`user.name` — the smoke-suite fixtures `git commit`), run `bash tests/test-scripts.sh` then `bash scripts/gate.sh`.

All shell must pass `shellcheck` and use `set -uo pipefail` where scripts already do. The gate enforces shellcheck + the smoke suite.

When done:
1. Run `scripts/gate.sh` from the repo root — it must pass.
2. Commit your work on this branch (include `work/work-profile/plan.md`, already present) with a clear message.
3. Print a final summary: what changed and why, any criteria only partially met, out-of-scope observations.
