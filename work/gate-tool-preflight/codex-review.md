No substantive findings.

The implementation matches the approved footprint and acceptance criteria: skipped checks are visible only when the stack marker is present, required-tool preflight aborts before checks, config/env behavior is covered, and docs/ARCHI are updated consistently with the new count.

I did not run the full gate because this review is against the provided plan/diff in a read-only checkout.

Codex verdict: APPROVE
